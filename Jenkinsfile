pipeline {
    agent any

    stages {
        stage('DOCKER CONTAINER STATUS') {
            steps {
                sh'/home/ubuntu/jenkins_docker/start.sh'
                
            }
        }
    }
}
